# This is a basic workflow to help you get started with Actions

name: AMPRODUCTS
name: AMPRODUCTS

on:
  push:
    branches: [no branches currently]
  pull_request:
    branches: ["only on social media"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello, world!

      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.

      - name: Use the marketing template
        run: |
          # Replace this with the actual command or script to use the marketing template
          echo "Use the marketing template to create engaging content."
          echo "Customize it with your product details and branding."


